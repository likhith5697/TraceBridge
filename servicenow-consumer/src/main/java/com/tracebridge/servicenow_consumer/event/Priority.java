package com.tracebridge.servicenow_consumer.event;

// Own copy of the priority vocabulary - deliberately not shared code with service-request-api.
public enum Priority {
    P1,
    P2,
    P3,
    P4
}
