package com.tracebridge.customer_db_consumer.event;

// Own copy of the priority vocabulary - deliberately not shared code with the other services.
public enum Priority {
    P1,
    P2,
    P3,
    P4
}
